tasks:
  - name: NextJS Frontend
    init: cd /workspace/nextjs-frontend && yarn
    command: cd /workspace/nextjs-frontend && yarn run dev
  - name: Discord Bot Backend
    init: cd /workspace/python-discord-bot && pip install -r requirements.txt
    command: cd /workspace/python-discord-bot && python bot.py

ports:
  - name: Open Me!
    port: 3000
    onOpen: open-browser
    visibility: public